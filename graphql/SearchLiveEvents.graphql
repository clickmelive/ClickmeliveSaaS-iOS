query SearchLiveEvents($input: LiveEventSearchInput!) {
    searchLiveEvents(
        input: $input
    ) {
        metadata {
            ...MetadataGQL
        }
        liveEvents {
            ...LiveEventGQL
        }
    }
}
